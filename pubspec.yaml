name: localizations_app
description: "A new Flutter project."

publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter

  # core packages
  equatable: ^2.0.7

  # Cache Packages
  shared_preferences: ^2.5.3

  # Localization Packages
  flutter_localizations:
    sdk: flutter
  intl: ^0.19.0

  # Iconsax Packages
  iconsax: ^0.0.8
  cupertino_icons: ^1.0.8

  # State Management Packages
  provider: ^6.1.4
  flutter_bloc: ^9.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:

  uses-material-design: true

  assets:
    - assets/translations/